// Root page — next-intl middleware handles locale routing automatically.
// When defaultLocale is 'ja' and localePrefix is 'as-needed',
// the root `/` serves the Japanese content via `app/[locale]/page.tsx`.
export default function RootPage() {
  return null;
}
